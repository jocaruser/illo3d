# Dependencies
node_modules

# Build output
dist
dist-e2e

# Environment & secrets
.env
.env.local
.env.*.local
*.pem
*.key

# IDE & OS
.idea
.vscode
*.swp
.DS_Store

# Logs & debug
logs
*.log
npm-debug.log*

# Test & coverage
coverage
.pnpm-store

# OpenSpec change drafts (local only; do not commit unless explicitly force-added)
openspec/changes/

illo3d-366398a927d2.json
test-results/.last-run.json
.cursor/mcp.json
.github-token
tsconfig.app.tsbuildinfo
tsconfig.node.tsbuildinfo
test-results/
/public/fixtures/
.e2e-fixtures/
tests/e2e/.auth/
fixtures/realdata
.gitignore
.opencode/
# docs/ stays untracked except the framework-managed capability details,
# which FRAMEWORK.md links to and which must ship with the repo.
docs/*
!docs/aircury/
opencode.json

# google-drive-api-mock data directories (pnpm run google-mock / e2e service)
.google-mock-data/
.e2e-google-mock/

# Local playground / sandbox files
playground/
.e2e-fixtures-*/
test-results-*/
tests/e2e/.auth/storage-state-*.json

# Aircury AI Framework
specs/changes/
