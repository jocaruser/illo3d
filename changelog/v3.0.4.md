# v3.0.4

## Fixed

- Local folder reopen no longer stays stuck on “checking” when the browser’s permission probe fails; the re-allow overlay is shown instead so you can grant access or return to the welcome screen.
