## Damaged local shop metadata

- Picking a local folder whose `illo3d.metadata.json` exists but is corrupt or invalid no longer offers to create a new shop (which could overwrite existing files). The welcome flow shows the same structure error used for broken shop layouts instead.
