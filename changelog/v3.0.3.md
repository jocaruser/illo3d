# v3.0.3

## Profile sign-out

Sign-out from the profile menu now asks to discard unsaved workbook changes
when a shop is open, matching Refresh behaviour.

Completing sign-out clears the persisted local CSV folder handle so the
welcome flow does not silently reuse the previous folder permission.
