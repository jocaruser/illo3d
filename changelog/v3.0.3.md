## Local folder reopen

- When a persisted local shop reloads but folder access has lapsed, the app shows a blocking re-allow prompt instead of only a load error toast.
- Granting access again reopens the same folder without the system picker; declining returns to the welcome screen and clears the persisted handle.
