# v3.0.3

## Job detail notes copy

- Shared `specs/shared/notes.spec.md` and `specs/shared/tags.spec.md` imported from reconciliation work.
- Job detail notes use `jobDetail` severity labels and save confirmation (English and Spanish), matching client detail behaviour.

## Migration wizard

The migration dialog now describes upgrade benefits for each major hop your shop
still needs, in order, instead of always showing the version 1→2 story.
English and Spanish copy follow the reconciled per-hop migration specifications.
