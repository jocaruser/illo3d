# v2.0.0

**Released:** TBD

## What's Changed
* feat: add `audit_log` entity table and `AuditEntry` type for mutation tracking by @jocaruser
  * New `AuditEntry` interface with `id`, `timestamp`, `actor`, `entity_name`, `entity_id`, `action`, `before_json`, `after_json`, `fieldsChanged`, `parent_entity_name`, `parent_entity_id`
  * New `AuditEntityName` and `AuditAction` union types
  * `audit_log` registered as a first-class sheet in `SHEET_NAMES` and `SHEET_HEADERS`
  * Empty `audit_log.csv` fixtures added to all fixture directories (`empty`, `happy-path`, `imports`, `kanban-stale-jobs`, `missingcolumn`)
* feat: add Audit Log page UI scaffolding
  * New protected route at `/audit-log`
  * Navigation link in header
  * Breadcrumb support (Home > Audit Log)
  * Page shell with title "Audit Log" and placeholder table showing all `AuditEntry` column headers
  * Loading spinner and empty state ("No audit entries yet")
  * i18n keys for English and Spanish
  * E2E coverage: navigation and direct load tests
