## Shared dropdown contract

- Added `specs/shared/dropdown.spec.md` and ADR-0017 for fuzzy matching across search surfaces.
- Combobox option filtering now uses the shared `fuzzyFilter` matcher.
- Audit log filters, purchase category, and note severity pickers use Combobox instead of native Select for consistent searchable behaviour.
