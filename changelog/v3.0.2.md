# v3.0.2

## Client detail jobs table

- The embedded jobs table on a client page now shows a **Total** column (with the same
  formatted amount or incomplete-pricing badge as the main jobs list) instead of due date.
- Total column is sortable with incomplete-pricing rows grouped at the bottom, matching `/jobs`.
