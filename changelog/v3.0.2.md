# 3.0.2

## Shared table viewport tiers

- Added `specs/shared/table.spec.md` and column viewport documentation for
  in-scope list and detail tables.
- Centralised responsive column visibility through `tableViewportTierClass` and
  per-column `viewportTier` props on shared table primitives (behaviour
  unchanged at each breakpoint).
