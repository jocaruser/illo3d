## Shared list specs and discovery hook

- Land reconciled `specs/shared/` list, search-box, and table behaviour specs plus client and job list cross-links.
- Add `useListTableDiscovery` so top-level list pages share one search harness; export `filterRowsBySearchQuery` as an alias of `fuzzyFilter`.
- Add ADR-0017 (fuzzy matching in every search) and ADR-0014 (archive lifecycle) under `specs/decisions/`.
