# BudgetPage mockups

Form: Printer (dropdown), Filaments section (filament + grams per row, up to printer max), Consumables section (item + quantity). Primary CTA: Calculate. Result block: Total cost, Markup %, Suggested price (accent for suggested price).
