# Development Log - Version 1

## 2026-01-06

### What We Accomplished
- Set up project documentation and AI workspace
- Scoped app requirements and v1-v5 roadmap
- Selected tech stack: React, PrimeReact, PWA
- Established GPL v3 licensing
- Designed database structure and entity schemas
- Defined v1 page structure and UX flow
- Added [DRAFT] features workflow for undecided items
- Established code standards and documentation requirements

### What's Unfinished
- Design kanban entity schema
- Choose kanban library
- Set up project structure

## 2026-01-30

### What We Accomplished
- Skeleton: Vite + React + TS, Vitest + RTL, Docker dev env, docs/database-schema.json
- Atoms (Button, Input, Label) and layouts (Nav, VerticalCentered, HorizontalCentered, Box, List, InputLayout) with co-located CSS modules and tests
- Login page with layout tree and Google OAuth placeholder; requirements and AI rules for frontend tree, palette, no new atom without check
- docs/frontend-tree.md, docs/design.md (logo-derived colour palette), commit-flow rules (tests + devlog resummarize)

### What's Unfinished
- Design kanban entity schema; choose kanban library; set up project structure

## 2026-01-31

### What We Accomplished
- GitHub Actions test workflow (push/PR to main); gh CLI in Docker for checking runs
- Token for gh in ~/.config/illo3d/gh.env (outside project); .env in gitignore; docs/context and AI rules updated
- CRM: Version 2 Client profiles (requirements), Client entity and Sale.clientId (context + database-schema.json)
- Backup strategy: decided (app copies, export, copy in Drive with triggers, import replace-only); documented in context and requirements (V3 Backup and data portability)

### What's Unfinished
- GitHub Action tests still failing (investigate)
- Design kanban entity schema; choose kanban library; set up project structure

## 2026-01-31 (session: layout and theme)

### What We Accomplished
- Layout and theme slice (V1 shell): react-router-dom; BrowserRouter with routes (/, /inventory, /budget, /login); AppLayout with side nav (NavLayout), nav links (Home, Inventory, Budget, Login), theme toggle, and Outlet for route content
- Theme: CSS variables for light/dark in index.css ([data-theme="light"] / [data-theme="dark"]) per design.md; ThemeContext with localStorage (illo3d-theme), toggle, and data-theme on document; initial theme applied in main.tsx before mount to avoid flash
- Placeholder pages: HomePage, InventoryPage, BudgetPage (minimal heading + text); LoginPage kept at /login
- docs/frontend-tree.md updated (contexts/ThemeContext, layouts/AppLayout, new pages); AppLayout.test.tsx for nav links, theme toggle, and outlet

### What's Unfinished
- (Unchanged) GitHub Action tests; kanban; project structure

## 2026-01-31 (session: auth and shell)

### What We Accomplished
- Dark theme by default (ThemeContext, index.html, main.tsx)
- Auth: AuthContext with token in localStorage; menu (AppLayout) only when logged in; protected routes redirect to /login when no token
- Login placeholder: single button saves dummy token and navigates to home; LoginPage without nav; AppLayout shows Log out instead of Login
- docs/frontend-tree.md (AuthContext); tests updated for AuthProvider and token

### What's Unfinished
- (Unchanged) GitHub Action tests; kanban; project structure

## 2026-02-10

### What We Accomplished
- docs/mockups/ structure mirroring src/pages/ (HomePage, InventoryPage, BudgetPage); mockups light + dark with palette
- i18n: I18nContext, t(key), src/locales/en.json only; user-facing strings via t(); rules and docs updated
- PrimeReact: primereact + primeicons; Lara theme (light/dark); Button, Input, Checkbox as thin wrappers; TabsLayout with TabView; InventoryPage tables with DataTable; TableLayout removed; docs/architecture and frontend-tree updated
- Auth: Sign in with Google via Google Identity Services (GIS); @react-oauth/google, client ID from VITE_GOOGLE_CLIENT_ID; LoginPage button and callback save ID token and navigate to /; AuthContext logout calls googleLogout; docs/architecture (app-shell-and-auth) and context.txt updated

### What's Unfinished
- (Unchanged) GitHub Action tests; kanban; project structure. Login page mockup deferred.

## 2026-02-10 (session: Drive and login)

### What We Accomplished
- Google Drive integration: DriveContext (request Drive scope, store access token); driveApi service (app data folder: readDatabase, saveDatabase, buildDatabaseFromInventory); Database type and DEFAULT_DATABASE; InventoryContext loadFromDatabase; HomePage Load/Save from Drive (when token present)
- Drive requested at login: after sign-in (One Tap or button), app requests Drive access and navigates to / only when Drive is granted; LoginPage uses useGoogleOneTapLogin and shows "Grant Drive access" message; logout clears Drive token (AppLayout clearDriveAccess)
- Removed separate "Connect Google Drive" from Home; docs/architecture/drive-and-data.md, app-shell-and-auth and context.txt updated; frontend-tree (DriveContext, services, types/database)

### What's Unfinished
- (Unchanged) GitHub Action tests; kanban; project structure.
