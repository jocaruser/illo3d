services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    working_dir: /app
    environment:
      - NODE_ENV=development
    command: tail -f /dev/null
    networks:
      - illo3d-network

networks:
  illo3d-network:
    driver: bridge
