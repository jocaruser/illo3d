# Google OAuth (get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-client-id-here

# No Google API key is required; the Picker feature is disabled.

# Fixtures root directory (default: public/fixtures)
# VITE_FIXTURES_ROOT=public/fixtures
