name: CI

on:
  pull_request:
    branches: [main]

env:
  DOCKER_BUILDKIT: 1

jobs:
  # ============================================================
  # JOB 1: Docker Images (Parallel builds using matrix strategy)
  # ============================================================
  docker-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [app, playwright]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ${{ matrix.image }} image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile${{ matrix.image == 'playwright' && '.playwright' || '' }}
          push: false
          load: true
          tags: illo3d-${{ matrix.image }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ============================================================
  # JOB 2: Lint (Fast feedback - no build required)
  # ============================================================
  lint:
    runs-on: ubuntu-latest
    needs: docker-build
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Start app container
        run: docker compose up -d --no-build app

      - name: Install dependencies
        run: make install

      - name: Lint
        run: make lint

  # ============================================================
  # JOB 3: Unit Tests (Fast feedback - no build required)
  # ============================================================
  unit-test:
    runs-on: ubuntu-latest
    needs: docker-build
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Start app container
        run: docker compose up -d --no-build app

      - name: Install dependencies
        run: make install

      - name: Unit tests
        run: make test

  # ============================================================
  # JOB 4: Build (TypeScript + Vite compilation)
  # ============================================================
  build:
    runs-on: ubuntu-latest
    needs: docker-build
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Start app container
        run: docker compose up -d --no-build app

      - name: Install dependencies
        run: make install

      - name: Build
        run: make build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 1

  # ============================================================
  # JOB 5: E2E Tests (Requires built app)
  # ============================================================
  e2e-test:
    runs-on: ubuntu-latest
    needs: [docker-build, build]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Start services
        run: docker compose up -d

      - name: E2E tests
        run: make e2e-test

  # ============================================================
  # Auto-merge (depends on all quality jobs)
  # Same-repo branches only: fork PRs get a read-limited token and must not be auto-approved here.
  # ============================================================
  auto-merge:
    runs-on: ubuntu-latest
    needs: [lint, unit-test, build, e2e-test]
    if: ${{ !github.event.pull_request.draft && github.event.pull_request.head.repo.full_name == github.repository }}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Approve pull request
        run: gh pr review --approve "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable merge-commit auto-merge
        run: gh pr merge --auto --merge "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
